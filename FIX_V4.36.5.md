# V4.36.5 水果识别与底部优先修正

## 证据与原因
- 上传日志 14:43:15 识别23个水果、7个可直接下落；14:43:24 剩余206→206，没有确认消除。
- 上传日志和本截图并非逐帧对应证据，不能据其坐标确定截图中被点击的具体水果。
- 工程根目录 FruitGameSolver 原版把全部对象送入配对，受阻只扣分。原日志的“可配对候选=7”其实打印的是可下落数，未反映参与配对的全部23个对象。
- 连通域最大尺寸0.175W；相接水果合并为大块后丢弃，既漏候选，又漏障碍物。
- 仅凭相似度总分0.990过滤，会拒绝本图中约0.982的底部葡萄对。

## 改动
1. 从当前帧完整水果提取模板，使用内部颜色纹理粗筛、全形状复核，补回相接水果；未使用固定点击坐标。模板来源仅为本帧，不用恢复结果再次生成模板。
2. 全量对象用于检查阻挡，受阻不再仅扣分。先选两颗都更靠下的可靠对子，再比较综合分；一对中先点更低的一颗。
3. 配对阈值固定为：总分≥0.975、RGB平均误差≤0.035、颜色直方图相似度≥0.990、形状IoU≥0.970，必须同时通过。不逐轮降低门槛。
4. B重定位也要求路径畅通。A仍在原位时，不因其它对象数变化而继续点击B。
5. 增加动画等待；消除剩余数不变/未识别时，无额外点击地复核一次OCR。
6. 求解上限从7分钟调整为30分钟，避免206颗水果逐对截图/OCR时过早超时；仍保留130对动作上限和人工中止。
7. Android版本号4.36.5/41，启动日志同步更新。

## 离线验证
直接编译实际FruitGameSolver.java，用桌面Bitmap/Context/OCR替身读取上传截图，分析尺寸720×1560。旧版21个候选，修改版43个候选（并非声称全部水果识别成功）。
首选：分析坐标(644,917)→(480,880)，即原图约(634,903)→(473,867)的底部两串葡萄；相似度0.982136。
回归断言：首选位置、B重定位、单对象无配对、屏蔽位置排除全部通过。
替身和结果位于offline-tests。Java17下复现：

```sh
mkdir -p /tmp/fruit-test-classes
java com.sun.tools.javac.Main -d /tmp/fruit-test-classes $(find offline-tests -name '*.java') app/src/main/java/com/zhinibgdu/xianyu/FruitGameSolver.java
java -cp /tmp/fruit-test-classes com.zhinibgdu.xianyu.Replay offline-tests/11873.jpg
```

## 使用与限制
这是源码工程，不是APK。打开压缩包内唯一的工程根目录；使用Android Studio构建，或沿用工程已有GitHub Actions构建流程。不要继续构建旧压缩包里的嵌套v4364副本。本交付包省略了原包重复的旧工程目录。
当前环境未做Android SDK整包构建；通过的是实际求解器的Java编译和单张截图回放。桌面缩放与Android Bitmap缩放可能有细微差异。
没有实机连续通关验证。静态截图不能证实游戏真实碰撞体、进入坑位的时刻或完整关卡规则；沿用的竖直下落+漏斗模型仍需实测。模板恢复依赖本帧至少有一个完整同类样本，对全部遮挡、尺寸变化、新布局可能失败。43个候选仍不是完整棋盘，遗漏障碍物的风险尚未消除。
安装自行构建的版本后，先观察首对，核对启动日志V4.36.5及剩余数是否206→204。若不变，保留A前、A后、B后截图和新日志用于下一轮定位，不能把对象减少直接当成通关。保留原有未知坑位停止机制。
