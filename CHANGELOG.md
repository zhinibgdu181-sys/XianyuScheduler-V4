# CHANGELOG

## 4.24.0 - 2026-09-16
- 首页新增权限状态区域：ROOT 权限卡片未授权时为灰色，获得 uid=0 后自动变为绿色并显示勾选。
- ROOT 卡片可点击重新检测；未授权时明确提示前往 KernelSU → 超级用户给“闲鱼定时助手”授权。
- 新增“闹钟权限”开关，实时读取 `AlarmManager.canScheduleExactAlarms()`；未授权时开关关闭/卡片灰色，授权后自动变绿。
- 点击闹钟开关会进入 Android 的“闹钟和提醒/精确闹钟”系统授权页面；返回应用后自动同步真实状态。
- ROOT 启动探测从 6 次改回 2 次快速复检，避免未在 KernelSU 授权时无意义等待；失败提示改为明确的 KernelSU 授权指引。
- 保留 V4.23 的页面分类修复、快速导航和游戏对象白名单规则。

## 4.23.0
- 修复闲鱼币主页仅因出现“点点消不停”卡片就被误判为麻将游戏。
- 页面分类改为强导航页面优先于小游戏标签。
- 新启动闲鱼时不再假定 InitActivity 一定落在首页；若恢复到旧 H5/夺宝/游戏子页，最多两次右侧边缘返回恢复。
- 极速导航新增“我的 → 闲鱼币 → 赚骰子”乐观链路，正常路径少一次整屏 OCR，失败自动回退到原识别流程。
- 修正运行日志版本号，便于确认当前安装版本。

## 4.22.0
- 游戏交互改为对象白名单。水果求解器只允许点击识别出的水果对象。
- 禁止自动点击“打乱”“消除”“解锁”“使用”等游戏功能按钮。
- 找不到高置信水果对子时直接安全停止并保留现场，不再借助游戏功能按钮。
- TaskExecutor 增加第二层硬拦截：FruitGameSolver 的非配对点击会被拒绝；麻将仅允许点击/滑动麻将对象。

## 4.21.0
- 水果 Solver SAFE_STOP 且游戏仍在前台时保留现场，禁止条件返回、普通恢复和任务页扫描乱操作。
- 快速扫描阶段增加小游戏硬守卫。
- 新启动闲鱼时跳过 UIAutomator 预热，直接点击底部“我的”，随后用 OCR 验证目标页面。

## 4.20.0
- 游戏分流升级为“模糊任务提示 + 实际页面类型优先”。
- 新增游戏求解器独占锁。
- 真人学习库降权为受控建议库，禁止自动回放普通 TAP/滑动。
- 首页到任务面板导航改为 OCR 单帧接力。
- 根目录改为单一 `CHANGELOG.md`。

## 4.19.0
- 新增麻将对子视觉求解器。

## 4.18.0
- 新增水果配对视觉求解器。
