# V4.17 真机验证重点

1. 从闲鱼币主页点击“赚骰子”时，日志应出现 `[导航V4.17] OCR精确点击` 或 `OCR粘连...只点左侧`。
2. 不应再进入“闲鱼币兑好礼”。若仍误入，应立即识别并只执行一次右侧返回，然后验证回到 COIN_HOME。
3. UIAutomator 只作为 OCR 失败后的兜底；单次最长约 2.8 秒，不再连续三次各卡 8 秒。
4. 真人触摸触发人工接管后，正在执行的 root 读取命令应尽快被终止，后面不得继续导航或点击。
5. 任务面板仍保留 V4.16 的立即连贯执行逻辑。
