# V4.37.0 整体优化说明

基于上轮V4.36.5修复版继续优化。版本名4.37.0，versionCode=42。本包为源码工程，不包含已编译APK。

## 主要修改

| 范围 | 原问题 | 当前处理 |
| --- | --- | --- |
| 水果点击坐标 | Host写死1440×3120相关像素范围，与求解器比例坐标不一致 | 每次截图回传真实尺寸，GameTapPolicy做归一化白名单检查；精确使用检测中心，取消随机抖动 |
| 消除确认 | OCR缺失或异常数量变化后仍可能进入下一对 | PairVerification只接受已知基线N→N−2，或原有明确完成页面判定；复核一次仍不确定则DIRTY停止 |
| A点击无效 | 原位水果仍在时可能换其它水果尝试，无法证明坑位为空 | 保留现场并停止，不盲点B或新对子 |
| OCR缓存 | 游戏验证可能复用通用OCR缓存 | 水果Host每次验证先使缓存失效；普通导航保留原缓存策略 |
| OCR调用次数 | 每对已验证，仍有每6对额外周期OCR | 去掉重复周期检查；保留逐对与必要的延迟复核 |
| OCR资源 | 每次创建/关闭识别器，超时即回收尚被识别任务使用的图片 | 同一次运行复用识别器；异步任务未完成时移交资源至完成回调释放；退出运行后关闭共享识别器 |
| 截图命令 | 等进程结束才读输出，可能堵塞；重复代码与清理不一致 | RootCommandRunner将不用的输出直接重定向，定期检查取消/超时，结束清理进程和流 |
| 水果截图 | 固定临时路径，异常缩放可能漏释放 | 每次唯一临时文件，finally清理；成功时明确转移Bitmap所有权 |
| 检测开销 | 不匹配模板仍计算全部像素误差 | 累积误差已超过总允许误差时提前结束；不改变原匹配门槛 |
| 配对开销 | 对每个组合重复查找下方阻挡 | 全量对象预计算可下落集合，再枚举对子 |
| 定时任务 | 停用后送达的旧闹钟仍能启动服务 | Receiver先检查启用状态；配置值限制范围；忽略空开机Intent |
| 前台服务 | 运行超过固定唤醒锁时限，销毁与后台工作缺少联动 | 有任务时周期续期有限时唤醒锁；完成回调主线程处理；服务销毁请求停止执行器 |
| 学习与进程退出 | 采样循环未检查线程中断，输出读取存在并发边界 | 增加中断检查；先停止采样再释放OCR；同步读取命令输出，异常清理子进程 |
| 日志 | 从任意字节裁剪可能切断中文字符；清理失败未准确返回 | 从下一个完整换行后保留日志；部分文件删除失败返回失败 |
| 工程维护 | 压缩包包含嵌套旧工程，容易构建错版本 | 只保留一个工程根；版本和启动日志同步；CI加入离线测试 |

水果检测继续采用V4.36.5的当前帧模板补检与底部优先排序。没有增加自动点击道具或盲目放宽匹配阈值。

## 已执行验证

1. 13份生产Java文件通过JDK17语法解析。注意：语法解析不等于Android整包编译。
2. 实际FruitGameSolver、MahjongGameSolver以及三个新增纯Java类，在Android测试替身下完成Java编译。
3. 上传11873.jpg回放：43个候选；首选分析坐标(644,917)→(480,880)，仍为底部两串葡萄。首选、B重定位、单对象排除、屏蔽位置排除断言通过。
4. RuntimeRegression共37项断言通过，覆盖多分辨率白名单、越界与非预期reason、206→204/未知计数/异常变化，以及命令成功、失败、大输出、超时、取消和中断。
5. 实际ScreenOcr配合可控ML Kit/Android替身，8项生命周期断言通过：正常结果、Bitmap释放、识别器复用、处理中取消、完成后释放、重新创建与会话关闭。
6. 19份Android XML可解析。
7. 一次桌面JVM检测微基准：同图720像素宽，预热后3次，中位数V4.36.5为127ms，本次为94ms。只代表此环境的检测函数测量，不代表手机整体提速比例；截图和OCR耗时另计。

完整离线结果在offline-tests/results.txt。

## 复现离线检查

需要JDK17和Bash。工程根目录执行：

```sh
bash offline-tests/run.sh
```

该脚本使用临时输出目录，不把测试替身打进Android应用。它不会连接手机，也不会模拟完整游戏物理状态。

## 构建与安装

1. 解压后打开唯一工程目录XianyuScheduler-V4.37.0。
2. 优先沿用已有GitHub Actions工作流：上传整个工程，运行Build Android APK，成功后下载xianyu-v4.37.0-debug构建产物。
3. 本地构建需要Android SDK 35、JDK17及与工程匹配的Gradle。原工程自带gradlew脚本但缺少完整wrapper文件，本次没有伪造wrapper；可使用已安装的Gradle执行`gradle assembleDebug`。原CI指定的Gradle/Android插件版本保持不变。
4. 安装后确认应用版本4.37.0，日志开头也是V4.37.0。若签名不同导致无法覆盖安装，请先保留现有配置与记录再处理，不要直接清空数据。

## 实机验收与限制

本环境没有Android SDK完整构建链和连接的手机，没有执行assembleDebug、安装、真实ML Kit OCR或连续通关。前台服务、实际Root权限、系统闹钟与各厂商后台行为仍需设备验证。

先检查定时开关关闭后不启动旧任务；手动启动水果游戏，观察第一对是否选择底部葡萄，剩余数是否206→204；继续观察几对后的重新定位。可在运行中切回助手，检查停止是否生效。检查日志中的检测耗时用于判断实际设备开销。

模板恢复仍依赖当前帧有完整同类样本；43个候选不代表识别了全部水果。游戏碰撞与漏斗模型来自原工程假设，未用真实游戏引擎验证。图片中的隐藏遮挡、缺少模板、新布局、旋转缩放或已有坑位水果仍可能导致停止或错误判断。未知状态时停止是为了避免继续累积错误，不能把本次优化解释为保证通关。
