# V4.38.0 水果小游戏三槽二消修复

## 已确认规则

本版本按实机连续截图重建水果小游戏状态机：

- 两个相同水果完成一次消除，`剩余`正常按 `N → N-2` 变化。
- 中央底部是临时槽，最多同时保存 3 个尚未配对的水果。
- 点击后的水果会先下落到中央槽；因此不能再用“原位置水果立即消失”作为点击是否成功的判据。
- 三槽已满时，下一步只能点击能与槽中某一水果直接配对的同类水果。

## 求解策略

1. 每帧同时重建棋盘水果、下落阻挡关系和三槽状态。
2. 槽中已有水果时，优先寻找一个当前可直接下落的同类水果，单击后立即完成二消。
3. 若没有槽位直配：
   - 槽位 0/1/2：只有在已经锁定一个完整、高置信、两颗均可下落的 A/B 对子时，才允许开始新对子。
   - 槽位 2：A 暂时占满第三槽，随后 B 必须与 A 二消回到 2 槽；禁止单水果试探。
   - 槽位 3：禁止引入任何新类型，只允许槽位直配。
4. A 点击后的成功判定改为“槽位数量/状态发生预期变化”，不再检查 A 是否还出现在旧坐标附近。
5. A 进入槽位后，B 在新棋盘上全局重新定位，并再次检查当前可下落性；不使用已经失效的旧 B 坐标。
6. 每次实际二消仍使用 OCR `剩余 N → N-2` 做闭环确认；无法确认时停止继续连点。

## 三槽视觉模型

三槽位于中央竖井。槽中水果相互接触后常会粘成一个大连通域，因此本版本不依赖连通域数量，而使用三个归一化纵向带的水果前景占比：

- X：`0.445W ~ 0.555W`
- TOP：`0.710H ~ 0.765H`
- MID：`0.765H ~ 0.815H`
- BOTTOM：`0.815H ~ 0.855H`
- 前景占比阈值：`0.20`

稳定状态必须满足从下往上连续占用：`0=[]`、`1=[BOTTOM]`、`2=[MID,BOTTOM]`、`3=[TOP,MID,BOTTOM]`。如果出现上层占用而下层为空，则视为水果仍在掉落动画中，本轮不点击。

槽中水果与棋盘水果使用 HSV 直方图做类型匹配；棋盘目标仍必须通过原有的完整下落阻挡检查。

## 离线验证

加入用户提供的真实连续截图回归：

- 空槽：0 个
- 单水果槽：1 个
- 双水果槽：2 个
- 满槽：3 个
- 1/2/3 槽状态均能在棋盘中找到高置信直接配对目标
- `GameTapPolicy` 已加入 `水果游戏-槽位匹配` 白名单
- 原有 Java 语法、运行时策略、视觉回放和 OCR 生命周期回归继续执行

说明：当前工程缺少 `gradle/wrapper/gradle-wrapper.jar`，因此本环境不能执行完整 Android Gradle APK 构建；已执行工程自带的离线 Java 回归。最终仍需在实机上验证点击坐标、动画时序及完整第一关通关。
