# V4.19 validation

## 静态截图验证

使用用户提供的麻将教程真实截图 `1789538185127.jpeg` 验证视觉几何：

- 自动检测深青色棋盘边界：PASS。
- 推断6x6网格：PASS。
- 检测占用格：16张麻将牌。
- 高置信度同牌匹配：8组，和截图可见牌型对应。
- 首选动作：教程要求的水平相邻“中/中”对子。
- 最近的错误相似候选与真实对子存在明显距离，因此阈值使用保守 `RGB MAD <= 0.115`。

## Java检查

- `MahjongGameSolver.java` 使用最小 Android stub 执行 javac：PASS。
- `TaskExecutor.java`（含 V4.19 Host 接口和调用链）使用 Android/项目 stub 执行 javac：PASS。
- versionCode = 19 / versionName = 4.19.0。

## 真机必须观察

1. 进入点点消不停后出现 `[麻将V4.19] ✅ 棋盘定位成功`。
2. 教程首步应优先出现 `相邻对子 ... A=(2,1) B=(2,2)` 附近的日志。
3. 每次成功应看到 `✅ 消除成功 / 牌数 N -> N-2`。
4. 非相邻对子应出现 `直线路径` 或 `自由拖动`，动作后必须重新截图验证。
5. 任何动作无变化不能连续重复同一个动作。
6. 若游戏实际限制与当前拖动模型不同，应安全停止并把日志发回调整，不应盲目完成标记。
